3 cosas que aprendí en GIT hasta ahora

1. Arrancar un GIT o convertir una carpeta en git creando una carpeta oculta .git que desde ese momento comienza registrar todos los cambios o ajustes o versiones por ejemplo de un codigo, esto con el comando: git init

2. Hacer un commit con el comando: git commit -m "Primer commit: README inicial" en este caso fue un comentario puede ser una nueva version de codigo 

3. Subirlo a github online osea hacer push con comando: git push -u origin main aunque antes prepare el cambio para la subida con git add
